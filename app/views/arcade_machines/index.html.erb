<p id="notice"><%= notice %></p>

<h1>Winnitrons</h1>
<em>as far as the eye can see</em>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @arcade_machines.each do |arcade_machine| %>
      <tr>
        <td><strong><%= arcade_machine.name %></strong></td>
        <td><%= link_to 'Show', arcade_machine %></td>
        <td><%= link_to 'Edit', edit_arcade_machine_path(arcade_machine) %></td>
        <td><%= link_to 'Destroy', arcade_machine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Winnitron', new_arcade_machine_path %>
