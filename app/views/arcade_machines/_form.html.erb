<%= form_for(@arcade_machine) do |f| %>
  <% if @arcade_machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arcade_machine.errors.count, "error") %> prohibited this arcade_machine from being saved:</h2>

      <ul>
      <% @arcade_machine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name, required: true %>
  </div>

  <div>
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
