<div class="col-lg-12 playlist-list">
  <% playlists.each do |playlist| %>
    <div class="playlist card col-sm-2">
      <%= link_to playlist.title, playlist_path(playlist) %>
    </div>
  <% end %>
</div>